# Developement setup

This project uses devenv for developement setup.

Simply run:

```bash
devenv shell
```

Inside the shell, run android studio with command:

```bash
android-studio
```

Before submitting a PR, make sure the app builds (`./gradlew assembleRelease`).
